// main.js

function fakeKYC() {
  document.getElementById("kycResult").innerText = "🔐 CCCD / BLX 2025 đã tạo thành công với QR + NFC!";
}

function genAIPhoto() {
  document.getElementById("kycResult").innerText = "🧠 Khuôn mặt AI đã tạo xong. Có thể dùng cho KYC.";
}

function generateQRCode() {
  document.getElementById("kycResult").innerText = "📷 Mã QR CCCD đã được sinh ra và có thể check thật.";
}

function generateNFC() {
  document.getElementById("kycResult").innerText = "📡 Dữ liệu NFC đã được giả lập thành công.";
}

function startFakeVPN() {
  document.getElementById("vpnStatus").innerText = "🌍 VPN + IP + User-Agent đã được thay đổi!";
}

function generateAccounts() {
  document.getElementById("accountStatus").innerText = "✅ Tạo thành công tài khoản FB, TikTok, Shopee, Ngân hàng, YouTube, sàn ảo...";
}

function autoReup() {
  document.getElementById("reupStatus").innerText = "📤 Video reup + SEO + voice AI đã hoàn tất.";
}

function faceSwap() {
  document.getElementById("swapStatus").innerText = "🪄 Ảnh đã được ghép mặt xong - dùng xác minh KYC được.";
}

function generateDocs() {
  document.getElementById("docStatus").innerText = "📝 Đã tạo giấy tờ giả lập đầy đủ: CCCD, BLX, bằng cấp, hóa đơn...";
}

function aiLearn() {
  document.getElementById("aiStatus").innerText = "🤖 AI đang cập nhật dữ liệu & tự tối ưu theo hành vi người dùng...";
}

function cloneDevice() {
  document.getElementById("deviceStatus").innerText = "📱 Đã clone thiết bị: Android ID, User-Agent, GPS, model...";
}

function exportData() {
  document.getElementById("dataStatus").innerText = "💾 Xuất dữ liệu thành công!";
}

function importData() {
  document.getElementById("dataStatus").innerText = "📥 Nhập dữ liệu thành công!";
}

function autoCreateFaceWithAI() {
  document.getElementById("faceAIStatus").innerText = "🧬 Gương mặt AI + biểu cảm ngẫu nhiên đã được tạo ra từ dữ liệu mẫu.";
}

function generateBankCCCD() {
  document.getElementById("kycResult").innerText = "🏦 CCCD đã tạo tương thích API ngân hàng, xác minh được + hỗ trợ nạp/rút.";
}

function runOfflineMode() {
  document.getElementById("offlineStatus").innerText = "📡 Chế độ offline đang hoạt động, không cần kết nối mạng!";
}

function detectAntiFake() {
  document.getElementById("protectStatus").innerText = "🛡️ Bypass AI kiểm tra ảnh giả - an toàn hơn khi KYC & tạo tài khoản.";
}

function generateUIDAuto() {
  document.getElementById("uidStatus").innerText = "🧾 UID cá nhân mạng xã hội + ví điện tử đã được sinh tự động & lưu trữ.";
}
